/**
 * Sidebar for new app layout — same visual structure as sahaya-operations-suite,
 * same spacing, typography, hover, active glow. Production nav items, routes, auth unchanged.
 */
import { Sidebar } from './Sidebar';

export function SidebarNew() {
  return <Sidebar />;
}
